# Phase 1 - T2-3: 관리자 주요 기능 화면 QA 체크리스트

**작성일**: 2025년 11월 13일  
**작업 범위**: `/admin` 하위 주요 기능 화면 안정성 보강  
**브랜치**: `feat/fix-first-20251113`  
**목표**: 렌더링 안정성 + 기본 UX 보장 (기능 완성 X)

---

## 📋 체크리스트 사용 방법

### 1단계: 개발 서버 시작
```powershell
pnpm dev
```
서버 URL: `http://localhost:3000`

### 2단계: Mock 관리자 로그인
브라우저 콘솔(F12):
```javascript
localStorage.setItem('mockUser', JSON.stringify({
  uid:'admin-001',
  email:'admin@hyunpungkalguksu.com',
  displayName:'관리자',
  role:'owner',
  storeId:'store-hyunpung'
}));
localStorage.setItem('mockRole','owner');
location.href='/admin';
```

### 3단계: 콘솔 에러 수집기 설치
```javascript
window.__QA__=(()=>{
  const errors=[];
  const unhandled=[];
  const push=(arr,type,args)=>arr.push({
    type,
    args:[...args],
    time:new Date().toISOString(),
    path:location.pathname
  });
  const orig=console.error;
  console.error=function(...a){
    push(errors,'console.error',a);
    return orig.apply(console,a);
  };
  window.addEventListener('error',e=>push(errors,'error',[e.message,e.error]));
  window.addEventListener('unhandledrejection',e=>unhandled.push({
    reason:e.reason,
    time:new Date().toISOString(),
    path:location.pathname
  }));
  return{dump:()=>({errors,unhandled})};
})()
```

### 4단계: 화면별 순회 테스트
사이드바에서 각 메뉴를 클릭하며 아래 체크리스트 확인

### 5단계: 결과 확인
```javascript
window.__QA__.dump()
```

**성공 기준**:
```json
{
  "errors": [],
  "unhandled": []
}
```

---

## 🎯 화면별 체크리스트

### 1. Dashboard (대시보드)
**URL**: `/admin`  
**컴포넌트**: `src/pages/admin/Dashboard.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 4개 KPI 카드 표시 (오늘 매출, 주문, 평균 평점, PWA 설치율)
- [ ] 로딩 상태 표시 (spinner/skeleton)
- [ ] "실시간 주문 현황" 카드 표시
- [ ] "최근 리뷰" 카드 표시
- [ ] "시간대별 주문 현황" 차트 영역 표시

#### 기능 체크
- [ ] Mock 데이터 로딩 (약 800ms 후)
- [ ] 통계 숫자 정상 표시 (1,250,000원, 42건, 4.8, 68%)
- [ ] 빈 상태 메시지 표시 ("새로운 주문이 없습니다")

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `TypeError` 없음
- [ ] React key warning 무시 가능

---

### 2. Orders (주문 관리)
**URL**: `/admin/orders`  
**컴포넌트**: `src/pages/admin/Orders.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 상태별 통계 카드 6개 표시 (전체/접수대기/접수확인/조리중/완료/취소)
- [ ] 필터 탭 표시 (전체/접수대기/접수확인/조리중/완료/취소)
- [ ] 검색 입력창 표시
- [ ] 결제 방법 필터 표시
- [ ] 정렬 버튼 표시
- [ ] 주문 테이블 표시 (빈 상태 포함)

#### 기능 체크
- [ ] Mock 주문 목록 로딩
- [ ] 상태별 필터 클릭 시 목록 변경
- [ ] 검색어 입력 시 필터링
- [ ] 결제 방법 필터 동작
- [ ] 정렬 토글 (createdAt/totalAmount)
- [ ] 주문 상세보기 drawer 열림/닫힘
- [ ] 주문 취소 다이얼로그 열림 (사유 입력)
- [ ] 취소 사유 미입력 시 토스트 경고

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `order.payment?.method` null guard 동작
- [ ] `cancelDialog.order` null 체크 동작

---

### 3. Menus (메뉴 관리)
**URL**: `/admin/menus`  
**컴포넌트**: `src/pages/admin/Menus.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 통계 카드 표시 (전체 메뉴, 품절, 할인 중 등)
- [ ] "메뉴 추가" 버튼 표시
- [ ] "CSV 임포트" 버튼 표시
- [ ] 카테고리 필터 탭 표시
- [ ] 검색 입력창 표시
- [ ] 정렬 select 표시
- [ ] 메뉴 테이블 표시 (빈 상태 포함)

#### 기능 체크
- [ ] Mock 메뉴 목록 로딩
- [ ] 카테고리 필터 동작 (전체/닭칼국수/사이드/음료)
- [ ] 검색어 필터링
- [ ] 품절 토글 버튼 동작
- [ ] 메뉴 편집 다이얼로그 열림/닫힘
- [ ] 시간제 설정 다이얼로그 열림/닫힘
- [ ] 메뉴 생성 다이얼로그 열림/닫힘
- [ ] CSV 임포트 다이얼로그 열림/닫힘

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `menus.map` null guard 동작
- [ ] `stats` null 체크 동작

---

### 4. Reviews (리뷰 관리)
**URL**: `/admin/reviews`  
**컴포넌트**: `src/pages/admin/Reviews.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 통계 카드 표시 (평균 평점, 전체 리뷰, 이번 달 등)
- [ ] 평점 분포 그래프 표시
- [ ] 필터 select 표시 (전체/사진 첨부/신고된 리뷰)
- [ ] 정렬 select 표시 (최신순/평점 높은순/낮은순)
- [ ] 리뷰 카드 리스트 표시

#### 기능 체크
- [ ] Mock 리뷰 목록 로딩
- [ ] 필터 변경 시 목록 업데이트
- [ ] 정렬 변경 시 목록 재정렬
- [ ] 답글 작성 모달 열림/닫힘
- [ ] 답글 저장 동작
- [ ] 답글 삭제 동작
- [ ] 신고 다이얼로그 열림/닫힘
- [ ] 리뷰 숨기기 동작
- [ ] 더보기 버튼 동작

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `reviews.map` null guard 동작
- [ ] `reviews.find` null guard 동작
- [ ] `stats` null 체크 동작

---

### 5. Analytics (관제 대시보드)
**URL**: `/admin/analytics`  
**컴포넌트**: `src/pages/admin/Analytics.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 5개 KPI 카드 표시 (매출/주문/평점/설치율/전환율)
- [ ] "새로고침" 버튼 표시
- [ ] "리포트" 버튼 표시
- [ ] "일별 매출 추이" 차트 표시
- [ ] "시간대별 주문" 차트 표시
- [ ] "메뉴별 매출 Top 5" 차트 표시
- [ ] 데이터 집계 정보 카드 표시

#### 기능 체크
- [ ] Mock KPI 데이터 로딩
- [ ] 차트 데이터 렌더링
- [ ] 새로고침 버튼 클릭 시 데이터 재로딩
- [ ] 로딩 중 skeleton 표시

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `kpi` null 체크 동작 (로딩 중)
- [ ] `hourlyOrders.map` 동작
- [ ] `topMenus.map` 동작
- [ ] `dailySales.map` 동작

---

### 6. IntegratedAnalytics (통합 분석)
**URL**: `/admin/integrated-analytics`  
**컴포넌트**: `src/pages/admin/IntegratedAnalytics.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 기간 선택 버튼 표시 (주간/월간)
- [ ] "새로고침", "엑셀 다운로드" 버튼 표시
- [ ] KPI 카드 4개 표시 (매출/주문/쿠폰/할인)
- [ ] "주요 인사이트" 카드 표시
- [ ] "개선 제안" 카드 표시
- [ ] 탭 메뉴 7개 표시 (시간대별/메뉴/쿠폰/포인트/리뷰/배달/알림)
- [ ] 각 탭 내 차트/테이블 표시

#### 기능 체크
- [ ] Mock report 데이터 로딩
- [ ] 기간 변경 (주간/월간) 시 재로딩
- [ ] 새로고침 버튼 동작
- [ ] 엑셀 다운로드 버튼 동작
- [ ] 탭 전환 시 콘텐츠 변경
- [ ] 차트 렌더링 (Recharts)

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `report` null 체크 동작 (로딩 중)
- [ ] `report.insights` 배열 null guard 동작
- [ ] `report.recommendations` 배열 null guard 동작
- [ ] `report.topMenus` 배열 null guard 동작

---

### 7. Delivery (배달 관제)
**URL**: `/admin/delivery`  
**컴포넌트**: `src/pages/admin/Delivery.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 배달 비활성화 알림 표시 (설정 미완료 시)
- [ ] "배달대행사 설정" 버튼 표시
- [ ] 활성화 시: 통계 카드 4개 표시 (활성 배달/지연/완료/평균 시간)
- [ ] 탭 메뉴 3개 표시 (전체/진행중/지연)
- [ ] 배달 task 리스트 표시

#### 기능 체크
- [ ] Mock task 목록 로딩
- [ ] 설정 페이지 이동 버튼 동작
- [ ] 탭 전환 시 필터링
- [ ] 실시간 구독 (Mock)
- [ ] SLA 지연 계산 (45분 기준)

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `tasks` 배열 null guard 동작
- [ ] `activeTasks.filter` null guard 동작
- [ ] `settings` null 체크 동작

---

### 8. Promotions (쿠폰/프로모션)
**URL**: `/admin/promotions`  
**컴포넌트**: `src/pages/admin/Promotions.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 통계 카드 표시 (발급/사용/만료 등)
- [ ] "쿠폰 발급" 버튼 표시
- [ ] 쿠폰 리스트 표시 (빈 상태 포함)
- [ ] 발급 다이얼로그 표시 (버튼 클릭 시)

#### 기능 체크
- [ ] Mock 쿠폰 통계 로딩
- [ ] 쿠폰 발급 다이얼로그 열림/닫힘
- [ ] 발급 폼 입력 검증
- [ ] 쿠폰 발급 동작 (Mock)
- [ ] 발급 후 통계 재로딩

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `stats` null 체크 동작

---

### 9. Points (포인트 관리)
**URL**: `/admin/points`  
**컴포넌트**: `src/pages/admin/Points.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 기능 비활성화 알림 표시 (FEATURE_FLAGS.points=false 시)
- [ ] 활성화 시: KPI 카드 4개 표시 (전체 사용자/포인트/평균/활성)
- [ ] 포인트 정책 카드 표시
- [ ] 사용자 포인트 테이블 표시
- [ ] 조정 다이얼로그 표시 (버튼 클릭 시)

#### 기능 체크
- [ ] Mock balances 목록 로딩
- [ ] 통계 계산 (총 사용자/포인트/평균/활성)
- [ ] 조정 다이얼로그 열림/닫힘
- [ ] 포인트 조정 폼 검증
- [ ] 포인트 조정 동작 (Mock)

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `balances` 배열 null guard 동작
- [ ] `safeBalances` 계산 동작

---

### 10. Support (고객지원 채팅)
**URL**: `/admin/support`  
**컴포넌트**: `src/pages/admin/Support.tsx`

#### 렌더링 체크
- [ ] 페이지 로딩 시 하얀 화면/오류 없음
- [ ] 기능 비활성화 알림 표시 (FEATURE_FLAGS.support=false 시)
- [ ] 활성화 시: 통계 카드 3개 표시 (대기 세션/평균 응답/완료)
- [ ] 세션 리스트 표시 (좌측)
- [ ] 메시지 패널 표시 (우측)
- [ ] 탭 메뉴 3개 표시 (전체/진행중/종료)
- [ ] 메시지 입력창 표시

#### 기능 체크
- [ ] Mock sessions 목록 로딩
- [ ] 세션 선택 시 메시지 로딩
- [ ] 메시지 전송 동작 (Mock)
- [ ] 세션 종료/재개 토글
- [ ] 탭 필터링 (전체/진행중/종료)
- [ ] 미응답 세션 카운트

#### 콘솔 체크
- [ ] `console.error` 없음
- [ ] `sessions` 배열 null guard 동작
- [ ] `filteredSessions` 계산 동작
- [ ] `messages` 배열 null guard 동작

---

## 🔍 알려진 제한사항 (Phase 1)

### Mock 모드 동작
- **현재 상태**: `USE_FIREBASE = false` (하드코딩)
- **영향**:
  - 모든 데이터 localStorage 기반
  - 실시간 구독 미동작
  - API 호출 Mock 응답
  - 실제 저장 안 됨

### 기능 플래그 제한
- `FEATURE_FLAGS.support = false`: 고객지원 비활성화
- `FEATURE_FLAGS.points = false`: 포인트 비활성화
- `FEATURE_FLAGS.delivery = false`: 배달 추적 비활성화

### 차트 라이브러리
- Recharts 사용 (일부 차트만 구현됨)
- PieChart, Cell 등 미사용 import 있음 (삭제 가능)

### 인코딩 이슈
- `OrdersBoard.tsx`, `PaymentsSetup.tsx` 등 legacy 파일 한글 깨짐
- 현재 사용하지 않는 파일이므로 Phase 2에서 정리 예정

---

## 📊 수정 파일 목록

### Route 주석 추가 (10개 파일)
1. `src/pages/admin/Dashboard.tsx` - `/admin`
2. `src/pages/admin/Orders.tsx` - `/admin/orders`
3. `src/pages/admin/Menus.tsx` - `/admin/menus`
4. `src/pages/admin/Reviews.tsx` - `/admin/reviews`
5. `src/pages/admin/Analytics.tsx` - `/admin/analytics`
6. `src/pages/admin/IntegratedAnalytics.tsx` - `/admin/integrated-analytics`
7. `src/pages/admin/Delivery.tsx` - `/admin/delivery`
8. `src/pages/admin/Promotions.tsx` - `/admin/promotions`
9. `src/pages/admin/Points.tsx` - `/admin/points`
10. `src/pages/admin/Support.tsx` - `/admin/support`

### Null Guard 추가 위치

#### IntegratedAnalytics.tsx
- **Line ~229**: `report.insights.map` → `(report.insights || []).map`
- **Line ~248**: `report.recommendations.map` → `(report.recommendations || []).map`
- **Line ~330**: `report.topMenus.map` → `(report.topMenus || []).map`

#### Support.tsx
- **Line ~481**: `sessions.filter` → `(sessions || []).filter`
- **Line ~488**: `sessions.filter` (openSessions) → `(sessions || []).filter`
- **Line ~491**: `sessions.filter` (unreadCount) → `(sessions || []).filter`

#### Delivery.tsx
- **Line ~112**: `tasks.filter` → `(tasks || []).filter`

#### Reviews.tsx
- **Line ~107**: `reviews.find` → `(reviews || []).find`

#### Points.tsx
- **Line ~118**: `balances.filter` → `safeBalances.filter` (safeBalances 변수 추가)

---

## 🚀 Phase 2 개선 계획 (TODO)

### 전체
- [ ] Firebase 실제 연동 (`USE_FIREBASE=true`)
- [ ] 실시간 구독 활성화 (onSnapshot)
- [ ] 기능 플래그 동적 전환

### Dashboard
- [ ] 실시간 주문 알림 (FCM)
- [ ] 실시간 주문 목록 표시
- [ ] 최근 리뷰 실제 데이터 연동
- [ ] 시간대별 차트 구현

### Orders
- [ ] 주문 실시간 업데이트
- [ ] 주문 프린트 기능
- [ ] 주문 엑셀 다운로드
- [ ] 주문 통계 고도화

### Menus
- [ ] 메뉴 이미지 업로드
- [ ] 드래그앤드롭 정렬
- [ ] CSV 임포트 실제 파싱
- [ ] 메뉴 복사 기능

### Reviews
- [ ] 리뷰 이미지 슬라이더
- [ ] 리뷰 엑셀 다운로드
- [ ] 키워드 분석
- [ ] 감정 분석 (AI)

### Analytics
- [ ] 실시간 차트 업데이트
- [ ] 커스텀 날짜 범위 선택
- [ ] 비교 분석 (전월/전년)
- [ ] 리포트 예약 발송

### Delivery
- [ ] 실제 배달대행사 API 연동
- [ ] GPS 지도 표시
- [ ] 실시간 위치 추적
- [ ] SLA 알림

### Support
- [ ] 파일 첨부 기능
- [ ] 이모지 지원
- [ ] 세션 태그/메모
- [ ] 자동 응답 템플릿

---

## 📝 수동 검증 절차 요약

### 빠른 체크 (5분)
1. `/admin` 진입 → KPI 카드 4개 보이는지
2. 사이드바 11개 메뉴 순서대로 클릭 → 하얀 화면/붉은 에러 없는지
3. 콘솔에서 `window.__QA__.dump()` → errors 배열 비어있는지

### 정밀 체크 (30분)
1. 각 화면별 체크리스트 순서대로 확인
2. 버튼/모달/탭 모두 클릭해보기
3. 빈 상태 메시지 확인
4. 로딩 상태 확인
5. 콘솔 에러 기록

---

**작성 완료**  
2025년 11월 13일 23:30
