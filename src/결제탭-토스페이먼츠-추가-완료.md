# 💳 결제 탭 - 토스페이먼츠 추가 완료!

> **관리자 설정 > 결제 탭에서 나이스페이/토스페이먼츠 선택 가능**

---

## ✅ 완료 내용

### 이전 (Before)
```
❌ 나이스페이만 표시
❌ 결제사 선택 불가
```

### 현재 (After)
```
✅ 결제사 선택 UI 추가
✅ 나이스페이 / 토스페이먼츠 라디오 버튼
✅ 각 결제사별 설정 가이드
✅ 수수료 비교 표시
```

---

## 📸 화면 구성

### 1. 결제사 선택 카드

```
┌─────────────────────────────────────────┐
│  💳 결제사 선택                         │
│  사용할 결제 서비스 제공사를 선택하세요  │
├─────────────────────────────────────────┤
│                                         │
│  ○ 나이스페이        [전통적]           │
│     1998년 설립, 안정적인 결제 서비스   │
│     ✓ 높은 시장 점유율                  │
│     ✓ 다양한 결제 수단                  │
│     ✓ 금융권 신뢰도 높음                │
│                                         │
│  ○ 토스페이먼츠      [최신]             │
│     2021년 설립, 현대적인 결제 API      │
│     ✓ 경쟁력 있는 수수료                │
│     ✓ 빠른 정산 (D+1)                   │
│     ✓ 개발자 친화적 API                 │
│                                         │
│  ℹ️ 예상 수수료 (2024년 기준)           │
│     • 나이스페이: 신용카드 3.3~3.5%     │
│     • 토스페이먼츠: 신용카드 3.2%       │
│                                         │
└─────────────────────────────────────────┘
```

### 2. 나이스페이 설정 (선택 시)

```
┌─────────────────────────────────────────┐
│  상태                 설정/가이드       │
├─────────────────────────────────────────┤
│  NICEPAY 상태         CLI 설정 (서버)   │
│  ✅ 연결됨            firebase ...      │
│                                         │
│  필수 설정            NICEPAY 가이드     │
│  ✓ mid                MID/KEY 발급      │
│  ✓ key                Return/Cancel URL │
│  ✓ endpoint                             │
│                                         │
│  [상태 재확인]                          │
└─────────────────────────────────────────┘
```

### 3. 토스페이먼츠 설정 (선택 시)

```
┌─────────────────────────────────────────┐
│  상태                 설정/가이드       │
├─────────────────────────────────────────┤
│  토스페이먼츠 상태     CLI 설정 (서버)   │
│  ℹ️ Functions에 저장  firebase ...      │
│                                         │
│                       토스페이먼츠 가이드│
│                       API 키 발급       │
│                       테스트 모드       │
│                       테스트 카드 정보  │
│                       장점              │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🎨 주요 기능

### 1. 결제사 선택 라디오 버튼

```typescript
<RadioGroup value={selectedProvider} onValueChange={...}>
  {/* 나이스페이 */}
  <div className={선택시_하이라이트}>
    <RadioGroupItem value="nicepay" />
    <Label>
      나이스페이 [전통적]
      ✓ 높은 시장 점유율
      ✓ 다양한 결제 수단
      ✓ 금융권 신뢰도 높음
    </Label>
    {isConfigured && <CheckCircle2 />}
  </div>

  {/* 토스페이먼츠 */}
  <div className={선택시_하이라이트}>
    <RadioGroupItem value="toss" />
    <Label>
      토스페이먼츠 [최신]
      ✓ 경쟁력 있는 수수료
      ✓ 빠른 정산 (D+1)
      ✓ 개발자 친화적 API
    </Label>
  </div>
</RadioGroup>
```

### 2. 수수료 비교 알림

```typescript
<Alert>
  <AlertCircle />
  <AlertDescription>
    예상 수수료 (2024년 기준)
    • 나이스페이: 신용카드 3.3~3.5%
    • 토스페이먼츠: 신용카드 3.2%
    * 실제 수수료는 계약 협상에 따라 달라질 수 있습니다.
  </AlertDescription>
</Alert>
```

### 3. 조건부 렌더링

```typescript
{/* 나이스페이 설정 */}
{selectedProvider === 'nicepay' && (
  <NicePaySettings
    healthCheck={healthCheck}
    loading={loading}
    checking={checking}
    onRecheck={loadHealthCheck}
    onCopyCommand={copyCommand}
  />
)}

{/* 토스페이먼츠 설정 */}
{selectedProvider === 'toss' && (
  <TossPaymentsSettings
    onCopyCommand={copyCommand}
  />
)}
```

---

## 📋 나이스페이 설정 내용

### CLI 명령어
```bash
firebase functions:config:set \
  nicepay.mode="production" \
  nicepay.endpoint="https://api.nicepay.co.kr/v1" \
  nicepay.mid="YOUR_MERCHANT_ID" \
  nicepay.key="YOUR_MERCHANT_KEY" \
  nicepay.return_url="https://hp-kal.web.app/pay/return" \
  nicepay.cancel_url="https://hp-kal.web.app/pay/cancel"
```

### 가이드 항목
1. MID/KEY 발급
   - NICEPAY 개발자 센터 링크
2. Return/Cancel URL 등록
   - Return URL: https://hp-kal.web.app/pay/return
   - Cancel URL: https://hp-kal.web.app/pay/cancel

---

## 📋 토스페이먼츠 설정 내용

### CLI 명령어
```bash
firebase functions:config:set \
  toss.mode="production" \
  toss.client_key="live_ck_YOUR_CLIENT_KEY" \
  toss.secret_key="live_sk_YOUR_SECRET_KEY"
```

### 가이드 항목

#### 1. API 키 발급
- 토스페이먼츠 개발자 센터 링크

#### 2. 테스트 모드
```
Client Key: test_ck_...
Secret Key: test_sk_...
```

#### 3. 테스트 카드 정보
```
카드번호: 5570********1234
유효기간: 12/28
CVC: 123
```

#### 4. 토스페이먼츠 장점
```
┌──────────┬──────────┐
│ 수수료   │ 정산     │
│ 3.2%     │ D+1      │
├──────────┼──────────┤
│ API      │ 문서     │
│ RESTful  │ 매우 우수│
└──────────┴──────────┘
```

---

## 🎯 사용 방법

### Step 1: 관리자 페이지 접속
```
1. http://localhost:5173/admin
2. 좌측 사이드바 "설정" 클릭
3. "결제" 탭 선택
```

### Step 2: 결제사 선택
```
1. 나이스페이 또는 토스페이먼츠 선택
2. 선택 시 자동으로 해당 설정 가이드 표시
```

### Step 3: 설정 적용
```
나이스페이:
1. CLI 명령어 복사
2. 터미널에서 실행
3. firebase deploy --only functions

토스페이먼츠:
1. CLI 명령어 복사
2. 터미널에서 실행
3. firebase deploy --only functions
```

---

## 🔧 코드 변경 사항

### 파일: `/pages/admin/Settings/PaymentTab.tsx`

#### 1. 상태 추가
```typescript
const [selectedProvider, setSelectedProvider] = useState<PaymentProvider>('nicepay');
```

#### 2. 컴포넌트 분리
```typescript
// 기존: 단일 컴포넌트
export function PaymentTab() { ... }

// 변경: 3개 컴포넌트
export function PaymentTab() { ... }           // 메인
function NicePaySettings() { ... }            // 나이스페이
function TossPaymentsSettings() { ... }       // 토스페이먼츠
```

#### 3. 라디오 그룹 추가
```typescript
<RadioGroup 
  value={selectedProvider} 
  onValueChange={(value) => setSelectedProvider(value as PaymentProvider)}
>
  {/* 나이스페이 */}
  <div onClick={() => setSelectedProvider('nicepay')}>
    <RadioGroupItem value="nicepay" />
    ...
  </div>

  {/* 토스페이먼츠 */}
  <div onClick={() => setSelectedProvider('toss')}>
    <RadioGroupItem value="toss" />
    ...
  </div>
</RadioGroup>
```

---

## 📊 비교표

| 항목 | 나이스페이 | 토스페이먼츠 |
|------|-----------|-------------|
| 설립 | 1998년 | 2021년 |
| 안정성 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 개발자 친화성 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 수수료 (카드) | 3.3~3.5% | 3.2% |
| 정산 속도 | D+2~3 | D+1 |
| API 방식 | XML/SOAP | REST/JSON |
| 문서화 | 보통 | 우수 |
| 추천 대상 | 중대형 가맹점 | 스타트업/중소형 |

---

## ✅ 체크리스트

```
[ ] 개발 서버 재시작
[ ] /admin/settings 접속
[ ] "결제" 탭 선택
[ ] 나이스페이 라디오 버튼 클릭
    [ ] 나이스페이 설정 가이드 표시
    [ ] CLI 명령어 복사 버튼 작동
[ ] 토스페이먼츠 라디오 버튼 클릭
    [ ] 토스페이먼츠 설정 가이드 표시
    [ ] CLI 명령어 복사 버튼 작동
    [ ] 테스트 카드 정보 표시
    [ ] 장점 카드 표시
[ ] 수수료 비교 알림 표시
[ ] 선택 시 하이라이트 효과 확인
[ ] 반응형 레이아웃 확인 (모바일/데스크톱)
```

---

## 🎨 디자인 특징

### 1. 선택 하이라이트
```typescript
className={`
  ${selectedProvider === 'nicepay'
    ? 'border-[#D61C1C] bg-[#D61C1C]/5'  // 선택됨
    : 'border-gray-200 hover:border-gray-300'  // 선택 안됨
  }
`}
```

### 2. 뱃지 스타일
```
나이스페이:   [전통적]  - outline
토스페이먼츠: [최신]    - blue bg
```

### 3. 체크마크
```
나이스페이 설정 완료 시:
우측 상단에 초록색 체크마크 표시
```

---

## 🔗 참고 문서

```
내부 문서:
📖 PAYMENT-PROVIDER-선택-시스템-가이드.md
📖 결제사-선택-시스템-요약.md

외부 링크:
🔗 나이스페이: https://npg.nicepay.co.kr
🔗 토스페이먼츠: https://developers.tosspayments.com/
🔗 테스트 카드: https://docs.tosspayments.com/reference/test-card
```

---

## 💡 향후 개선 사항

```
1. Firestore 연동
   - 선택한 결제사 저장
   - 설정 상태 실시간 반영

2. 헬스체크 확장
   - 토스페이먼츠 상태 체크
   - API 연결 테스트

3. 자동 전환
   - 한 결제사 장애 시 자동 전환
   - 알림 발송

4. 통계
   - 결제사별 사용 통계
   - 수수료 비교 리포트
```

---

**작성:** KS컴퍼니 (사업자번호 553-17-00098)  
**일시:** 2024-11-08  
**상태:** ✅ 완료
