# 작업완료 보고서 - v1.0 STEP 1: ENV/플래그 정리 및 Firebase 설정 재점검

**작성일**: 2025-11-22  
**버전**: v1.0 (STEP 1)  
**브랜치**: `release/hpkal-v0.9.1` (기준 브랜치)  
**상태**: ✅ 완료

---

## 📋 작업 개요

### 목표

v1.0 Firebase 실연동을 위한 **환경 기초 구성** 완료. `USE_FIREBASE` 플래그를 환경 변수 기반으로 전환하고, Firebase ENV 구성 및 문서화를 설계 문서 수준까지 정리.

### 작업 범위

- ✅ `USE_FIREBASE` 플래그 환경 변수화 (`src/config/env.ts`)
- ✅ `.env.local.example` 예시 파일 생성
- ✅ `docs/ENVIRONMENT_SETUP.md` v1.0 설계 기준 전면 업데이트
- ✅ 빌드 검증 완료

**중요**: 이번 스텝에서는 **Firebase 로직 구현은 하지 않음**. ENV/플래그/설정 문서만 수정.

---

## 🔧 변경된 파일 목록

### 1. `src/config/env.ts` (수정)

**변경 내용**:
- `USE_FIREBASE` 하드코딩 제거 (`export const USE_FIREBASE = false;`)
- `VITE_USE_FIREBASE` 환경 변수 기반으로 전환
- 기본값: `false` (Mock 모드) 유지

**변경 전**:
```typescript
// Phase 1/2: 강제 Mock 모드 (LocalStorage)
// TODO: Phase 3에서 실제 Firebase 연동 시 ENV 기반으로 전환
export const USE_FIREBASE = false;
```

**변경 후**:
```typescript
// v1.0: Firebase 모드 플래그 (환경 변수 기반)
// VITE_USE_FIREBASE 환경 변수를 읽어서 boolean으로 변환
// 기본값: false (Mock 모드)
// true: Firebase 실연동 모드
// false: Mock 모드 (localStorage)
export const USE_FIREBASE = (() => {
  const raw = getEnv('VITE_USE_FIREBASE', 'false');
  if (raw === 'true') return true;
  if (raw === 'false') return false;
  // 기본값: false (Mock 모드)
  return false;
})();
```

**효과**:
- `.env.local` 파일에서 `VITE_USE_FIREBASE=true/false`로 Mock ↔ Firebase 모드 전환 가능
- 환경 변수가 없거나 잘못된 값이면 기본값 `false` (Mock 모드) 사용
- 기존 코드와의 호환성 유지 (기본값이 `false`이므로 기존 동작 유지)

---

### 2. `.env.local.example` (신규 생성)

**파일 경로**: 프로젝트 루트 `.env.local.example`

**내용 구성**:
1. **Firebase 모드 플래그**: `VITE_USE_FIREBASE=false` (Mock 모드 기본값)
2. **Firebase 설정**: Firebase Console에서 발급받은 값 입력 (7개 항목)
   - `VITE_FIREBASE_API_KEY`
   - `VITE_FIREBASE_AUTH_DOMAIN`
   - `VITE_FIREBASE_PROJECT_ID`
   - `VITE_FIREBASE_STORAGE_BUCKET`
   - `VITE_FIREBASE_MESSAGING_SENDER_ID`
   - `VITE_FIREBASE_APP_ID`
   - `VITE_FIREBASE_MEASUREMENT_ID` (선택)
3. **앱/스토어 기본 설정**: `VITE_APP_NAME`, `VITE_STORE_ID`
4. **기능 플래그**: 포인트, 배달, 지원, 결제 관련 (10개 항목)
5. **지도 API 키**: Kakao/Google Maps API 키 (선택)
6. **NICEPAY 설정**: 온라인 결제 사용 시 (선택)
7. **배달 대행사 설정**: 배달 연동 시 (선택)

**특징**:
- 각 섹션에 주석과 설명 포함
- 예시 값: `__FILL_ME__` (필수), `__OPTIONAL__` (선택)
- v1.0 설계 문서의 ENV 목록과 일치

---

### 3. `docs/ENVIRONMENT_SETUP.md` (전면 업데이트)

**변경 내용**:
- 기존 문서를 v1.0 설계 기준으로 전면 업데이트
- Mock 모드 ↔ Firebase 모드 전환 가이드 추가
- 모든 환경변수 목록 및 설명 정리 (표 형식)
- 회귀 테스트 전략 추가

**주요 섹션** (10개):
1. **개요**: Mock ↔ Firebase 모드 전환 가이드 목적
2. **모드 개요**: Mock 모드 vs Firebase 모드 특징 및 차이점
3. **ENV 파일 구성**: `.env.local.example`, `.env.local`, `.env.production` 역할
4. **필수 ENV 목록**: 모든 환경변수 목록 및 설명 (표 형식)
5. **모드 전환 방법**: Mock 모드 설정, Firebase 모드 설정, 회귀 테스트 전략
6. **스테이징/프로덕션 설정 전략**: Firebase Hosting 환경변수 설정 방법
7. **새 환경변수 추가 규칙**: 향후 환경변수 추가 시 따라야 할 단계
8. **릴리즈 전 체크리스트**: 개발/프로덕션 환경 최종 점검 항목
9. **트러블슈팅**: 대표적인 에러 및 해결 방법
10. **참고 문서**: 관련 문서 링크

---

## ✅ 검증 결과

### 빌드 검증

```bash
npm run build
```

**결과**: ✅ **성공**
- 빌드 시간: 14.85초
- 타입 에러: 없음
- 컴파일 에러: 없음
- 경고: Firebase 동적/정적 import 경고 (기존 경고, 이번 변경과 무관)

**결론**: ENV 관련 변경으로 인한 빌드 실패 없음. 모든 변경사항이 정상적으로 적용됨.

---

## 📊 USE_FIREBASE 동작 방식

### 변경 전

```typescript
export const USE_FIREBASE = false;  // 하드코딩
```

- Mock 모드로 고정
- 환경 변수로 전환 불가능
- 코드 수정 없이는 Firebase 모드 사용 불가

### 변경 후

```typescript
export const USE_FIREBASE = (() => {
  const raw = getEnv('VITE_USE_FIREBASE', 'false');
  if (raw === 'true') return true;
  if (raw === 'false') return false;
  return false;  // 기본값: Mock 모드
})();
```

**동작 흐름**:
1. `.env.local`에서 `VITE_USE_FIREBASE` 값 읽기
2. `'true'` 문자열이면 `true` (Firebase 모드)
3. `'false'` 문자열이면 `false` (Mock 모드)
4. 값이 없거나 다른 값이면 기본값 `false` (Mock 모드)

**사용 방법**:
```bash
# Mock 모드
VITE_USE_FIREBASE=false

# Firebase 모드
VITE_USE_FIREBASE=true
```

---

## 🎯 주요 성과

### 1. 환경 변수 기반 모드 전환 가능

- `.env.local` 파일만 수정하여 Mock ↔ Firebase 모드 전환 가능
- 코드 수정 없이 모드 전환 가능
- 회귀 테스트 용이

### 2. 예시 파일 제공

- `.env.local.example` 파일로 개발자가 쉽게 환경 변수 설정 가능
- 모든 필수/선택 환경변수 목록 및 설명 포함
- v1.0 설계 문서와 일치하는 구조

### 3. 문서화 완료

- `ENVIRONMENT_SETUP.md` 전면 업데이트
- Mock 모드 ↔ Firebase 모드 전환 가이드 완성
- 트러블슈팅 가이드 포함

---

## 📝 다음 단계 (STEP 2)

**STEP 2: Firestore 스키마 1차 구현**에서 이 ENV 설정이 사용되는 방식:

1. **Firebase 프로젝트 생성**:
   - Firebase Console에서 프로젝트 생성
   - `.env.local`에 `VITE_USE_FIREBASE=true` 및 Firebase 설정 값 입력

2. **Firebase 초기화 확인**:
   - `USE_FIREBASE=true`로 전환 시 Firebase 초기화가 정상 동작하는지 확인
   - 브라우저 콘솔에 `[Firebase] 초기화 시작 (USE_FIREBASE=true)` 로그 출력 확인

3. **Mock ↔ Firebase 전환 테스트**:
   - Mock 모드와 Firebase 모드를 쉽게 전환하여 개발 및 테스트 진행
   - 회귀 테스트 수행

---

## 🔍 체크리스트

### 완료된 작업

- [x] `USE_FIREBASE` 플래그 환경 변수화
- [x] `.env.local.example` 예시 파일 생성
- [x] `docs/ENVIRONMENT_SETUP.md` v1.0 설계 기준 전면 업데이트
- [x] 빌드 검증 완료
- [x] 타입 에러 없음 확인

### 다음 STEP에서 진행할 작업

- [ ] Firebase 프로젝트 생성 및 설정
- [ ] Firestore Database 생성
- [ ] Firestore 스키마 1차 구현 (menus/orders 중심)
- [ ] Firestore 보안 규칙 1차 작성

---

## 📚 관련 문서

- **v1.0 설계 문서**: `docs/v1.0_Firebase-실연동-설계초안_2025-11-22.md`
- **환경 설정 가이드**: `docs/ENVIRONMENT_SETUP.md`
- **예시 ENV 파일**: `.env.local.example`

---

## 💡 참고 사항

### 주의사항

1. **`.env.local` 파일은 Git에 커밋하지 않기**:
   - `.gitignore`에 `.env.*` 포함되어 있음
   - 실제 Firebase 설정 값은 절대 커밋하지 않기

2. **기본값 유지**:
   - `USE_FIREBASE`의 기본값은 `false` (Mock 모드)로 유지
   - 환경 변수가 없어도 기존 동작 유지

3. **Firebase 로직 구현은 STEP 2부터**:
   - 이번 STEP에서는 ENV/플래그/설정 문서만 수정
   - Firebase 로직 구현은 STEP 2부터 진행

---

**작성일**: 2025-11-22  
**작성자**: AI Assistant  
**상태**: ✅ **STEP 1 완료**  
**다음 단계**: STEP 2 (Firestore 스키마 1차 구현)

