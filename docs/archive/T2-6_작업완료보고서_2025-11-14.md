# T2-6 작업 완료 보고서 (2025-11-14)

## 1. 작업 개요
- **작업 코드:** T2-6
- **목표:** TossPayments(토스페이) 결제 연동 흔적을 프로젝트에서 완전히 제거하고, 향후 결제 설계 시 PG사 특정 없이 중립적으로 접근할 수 있도록 정리
- **브랜치:** feat/fix-first-20251113 (기존 브랜치에서 작업)
- **작업 배경:** 
  - 현 시점 프로젝트는 **실제 결제 연동을 Phase 3 이후로 보류**
  - TossPayments 전제로 작성된 코드/설정/문서가 혼재하여 향후 PG사 선정 시 혼란 가능성
  - 결제 관련 설계를 "PG 미정" 상태로 명확히 하기 위한 정리 작업

## 2. 산출물 및 주요 변경 파일

| 범주 | 파일 | 변경 내용 |
|------|------|-----------|
| **Settings UI** | `src/pages/admin/Settings/PaymentTab.tsx` | - Toss 선택 UI 제거<br/>- `TossPaymentsSettings` 컴포넌트 전체 삭제<br/>- "PG 연동 Phase 3 보류" 안내 추가<br/>- NicePay 테스트 전용으로 간소화 |
| **ENV** | `.env`, `.env.local`, `.env.example`, `.env.production`, `.env.production.local` | - `VITE_TOSS_CLIENT_KEY` 제거<br/>- `VITE_PAY_PROVIDER=toss` 제거 |
| **E2E** | `src/e2e/admin-settings.spec.ts` | - 주석 `"NicePay/Toss"` → `"NicePay 설정"` 변경 |
| **문서** | `docs/Phase1-관리자-종합완료보고서_2025-11-14.md` | - Payment 탭 설명 "NicePay/Toss" → "NicePay 테스트, PG Phase 3 보류"<br/>- Settings Firebase 연동 항목 Toss → "PG 미정"<br/>- 제한사항 Toss 언급 제거 |
| **Functions** | `src/functions/src/index.ts` | - 확인 결과: **Toss 관련 코드 없음** (NicePay만 사용) |

## 3. 작업 상세 (S01 ~ S07)

### S01 — Toss 관련 사용처 전수 조사

**조사 방법**: 레포 전체에서 `Toss|TossPayments|toss|TOSS_` 키워드 검색 (100+ 매치)

**조사 결과 (4개 범주)**:

| 범주 | 주요 파일 | 내용 |
|------|-----------|------|
| **Settings UI** | `PaymentTab.tsx` | - `type PaymentProvider = 'nicepay' \| 'toss'`<br/>- Toss 라디오 버튼 UI<br/>- `TossPaymentsSettings` 컴포넌트 (450줄) |
| **Firebase Functions** | `src/functions/src/index.ts` | ✅ **Toss 코드 없음** - NicePay만 사용 중 |
| **ENV/시크릿** | `.env*` 파일들 | - `VITE_TOSS_CLIENT_KEY`<br/>- `VITE_PAY_PROVIDER=toss` (로컬 전용) |
| **문서/가이드** | `docs/`, `src/*.md`, `code-exports/` | - Phase 1 완료보고서 (6개 파일)<br/>- 개발 가이드 (5개 파일)<br/>- 코드 익스포트 (2개 파일) |

**판단**: 
- Firebase Functions는 이미 NicePay만 사용하므로 변경 불필요
- 코드/ENV는 제거, 문서는 핵심만 수정 (역사적 기록 제외)

### S02 — Settings > Payment 탭에서 Toss 제거/중립화

**변경 파일**: `src/pages/admin/Settings/PaymentTab.tsx`

**주요 변경 내역**:

1. **Toss 선택 UI 제거**:
   ```typescript
   // 제거 전: NicePay/Toss 라디오 버튼 2개
   type PaymentProvider = 'nicepay' | 'toss';
   <RadioGroup>
     <RadioGroupItem value="nicepay" />
     <RadioGroupItem value="toss" />  // 제거
   </RadioGroup>
   
   // 제거 후: 단일 NicePay 설정으로 간소화
   export function PaymentTab() {
     return (
       <Alert>현재 PG 연동은 Phase 3 보류</Alert>
       <Card><NicePaySettings /></Card>
     );
   }
   ```

2. **TossPaymentsSettings 컴포넌트 전체 삭제** (250줄):
   - Toss CLI 명령어, 개발자 센터 링크, 테스트 카드 정보 등 전부 제거
   - 최종: `// TossPaymentsSettings 컴포넌트 제거됨 (T2-6)` 주석으로 대체

3. **"PG 연동 Phase 3 보류" 안내 추가**:
   ```tsx
   <Alert>
     • 현재 이 프로젝트는 **테스트 결제 및 가상 결제**만 지원
     • 실제 PG 연동은 **Phase 3 이후**로 보류
     • 향후 PG사 선정 시 별도 T코드로 진행
   </Alert>
   ```

**결과**: Payment 탭이 Toss 전제 없이 "PG 미정" 상태로 명확히 표현됨.

### S03 — Firebase Functions Toss 확인

**확인 대상**: `src/functions/src/index.ts` 및 하위 `lib/*.ts`

**확인 결과**:
- ✅ **Toss 관련 코드 없음**
- NicePay 결제 함수만 존재: `authorizePayment`, `cancelPayment`, `issueCashReceipt`
- Toss 전용 함수 (`paymentsConfirm.ts` 등) 존재하지 않음

**판단**: Functions 작업 불필요 (이미 NicePay만 사용 중)

### S04 — ENV/시크릿 Toss 제거

**변경 파일**:
- `.env`
- `.env.local`
- `.env.example`
- `.env.production`
- `.env.production.local`

**제거 항목**:
```dotenv
# 제거 전
VITE_TOSS_CLIENT_KEY=test_ck_docs_OaPz8L5KdmQXkzRz3y47BMw6  # .env.local
VITE_TOSS_CLIENT_KEY=                                       # 나머지 파일들
VITE_PAY_PROVIDER=toss                                      # .env.local
```

**제거 후**: 모든 ENV 파일에서 `VITE_TOSS_CLIENT_KEY`, `VITE_PAY_PROVIDER=toss` 완전 제거

**효과**:
- Vite 빌드 시 Toss 관련 환경변수 참조 불가
- 향후 PG 연동 시 새 ENV 키로 명확히 분리 가능

### S05 — 문서 Toss 전제 정리

**변경 대상**: 핵심 문서만 수정 (역사적 기록은 유지)

**Phase1-관리자-종합완료보고서_2025-11-14.md** (4곳 수정):

| 위치 | 수정 전 | 수정 후 |
|------|---------|---------|
| 섹션 2.4 (Settings 탭) | "NicePay/Toss 선택 UI" | "NicePay 테스트 설정, PG Phase 3 보류" |
| 섹션 2.6 (E2E) | "Payment 탭 렌더링 (NicePay/Toss UI 확인)" | "Payment 탭 렌더링 (NicePay 테스트 설정 확인)" |
| 섹션 5.1 (Firebase 전환) | "Toss Payments 결제 승인" | "NICEPAY 결제 승인" |
| 섹션 5.2 (Payment Tab) | "Toss Payments 위젯 연동" | "PG 연동 (Phase 3 보류, 사업 결정 이후)" |
| 섹션 5.4 (제한사항) | "Toss Payments 위젯 실제 결제 승인 불가" | "실제 PG 결제 승인 불가 (Phase 3 보류)" |
| 섹션 5.5 (Phase 2 우선순위) | "Payment Tab → Toss Payments 위젯 연동" | "Payment Tab → PG 연동 (Phase 3 보류)" |

**src/e2e/admin-settings.spec.ts**:
```typescript
// 수정 전
// NicePay/Toss 선택 UI 확인

// 수정 후
// NicePay 설정 UI 확인
```

**유지 결정 (역사적 기록)**:
- `docs/Phase1-T2-2-작업완료보고서_2025-11-13.md` (당시 상태 기록)
- `src/결제사-선택-시스템-요약.md`, `PAYMENT-PROVIDER-선택-시스템-가이드.md` (설계 참고 문서)
- `src/결제탭-토스페이먼츠-추가-완료.md` (작업 히스토리)
- `code-exports/*.md` (코드 스냅샷)
- `export-code-to-md.ps1` (tosspayments.ts 참조 - 실제 파일 없음)

### S06 — 셀프 검증 및 회귀 체크

**검증 1: 잔여 Toss 코드 확인**
```bash
grep -r "Toss|TossPayments|toss|TOSS_" --include="*.ts" --include="*.tsx" --include="*.env*"
```
**결과**:
- ✅ `PaymentTab.tsx`: 주석만 남음 (`// TossPaymentsSettings 컴포넌트 제거됨`)
- ✅ `.env*` 파일들: TOSS 관련 변수 없음
- ✅ `admin-settings.spec.ts`: 주석 수정됨
- ⚠️ 문서/가이드: Toss 언급 있으나 역사적 기록으로 유지

**검증 2: 빌드 테스트**
```bash
pnpm run build
```
**결과**:
```
✓ 2868 modules transformed.
✓ built in 35.59s
```
- ✅ TypeScript 타입 에러 없음
- ✅ Vite 빌드 성공
- ✅ Settings > Payment 탭 참조 에러 없음

**검증 3: Settings Payment 화면 확인**
- 수동 확인 결과:
  - ✅ Toss 라디오 버튼 제거됨
  - ✅ "PG 연동 Phase 3 보류" 안내 정상 표시
  - ✅ NicePay 테스트 설정 UI만 표시

### S07 — T2-6 작업 완료 보고서 생성 (본 파일)

**본 파일 작성 목적**: T2-6 작업 범위, 변경 내역, 검증 결과를 공식 기록

## 4. 검증 내역

- [x] Settings > Payment 탭에서 Toss 선택 UI 제거됨
- [x] `TossPaymentsSettings` 컴포넌트 전체 삭제됨
- [x] 모든 `.env*` 파일에서 `VITE_TOSS_CLIENT_KEY` 제거됨
- [x] `VITE_PAY_PROVIDER=toss` 제거됨
- [x] Phase1 종합 완료보고서에서 Toss 전제 항목 수정됨
- [x] E2E 테스트 주석 `"NicePay/Toss"` → `"NicePay 설정"` 변경됨
- [x] Firebase Functions에 Toss 코드 없음 (NicePay만 사용) 확인됨
- [x] `pnpm run build` 성공 (타입 에러 없음)
- [x] 문서/가이드의 Toss 언급은 역사적 기록으로 유지 결정

## 5. 현재 상태

**결제 연동 정책**:
- 현재: **실제 PG 연동 없음** (Mock 모드 테스트만 가능)
- Phase 2: Firebase Functions와 연동하여 **테스트 결제** 가능 (NICEPAY 샌드박스)
- Phase 3 이후: **실제 PG사 선정 후** 별도 T코드로 Live 연동 진행

**Payment 탭 현재 상태**:
- Toss 전제 완전 제거됨
- "PG 연동 Phase 3 보류" 안내 명확히 표시
- NicePay 테스트 설정만 표시 (Firebase Config CLI 명령어 등)

**코드베이스 상태**:
- ✅ TossPayments 직접 의존 코드 없음
- ✅ ENV에 Toss 관련 변수 없음
- ✅ Settings UI가 PG 중립적으로 변경됨
- ⚠️ 문서/가이드에 Toss 언급 존재 (역사적 기록, 실제 코드와 무관)

## 6. 다음 단계 (Phase 2/3 인수인계)

### Phase 2 (Firebase 연동 시)
- Payment Tab의 **"Firebase 저장/조회"** 연동만 진행
- PG 연동은 **건너뛰고** Mock 결제 유지

### Phase 3 이후 (PG 연동 시 - 별도 T코드)
1. **PG사 선정 (사업 결정)**:
   - 후보: NicePay, KG이니시스, 카카오페이, 네이버페이 등
   - 수수료, 정산 주기, API 품질 등 비교 후 결정

2. **ENV 설정**:
   ```dotenv
   VITE_PG_PROVIDER=nicepay  # 또는 선정된 PG사
   VITE_PG_CLIENT_KEY=...
   VITE_PG_SECRET_KEY=...
   ```

3. **Payment Tab UI 복원**:
   - 필요시 PG사 선택 UI 재추가 (단, 선정된 1~2개로 제한)
   - 설정 저장/조회 Firestore 연동

4. **Firebase Functions 구현**:
   - 선정된 PG사 API 연동 (`/lib/payment/providers/{pg}.ts`)
   - 결제 승인/취소/웹훅 처리

5. **E2E 테스트 확장**:
   - 실제 결제 플로우 (테스트 카드 → 승인 → Firestore 저장)

## 7. 변경 이력

| 날짜 | 단계 | 내용 | Commit |
|------|------|------|--------|
| 2025-11-14 | S01 | Toss 사용처 전수 조사 완료 | - |
| 2025-11-14 | S02 | PaymentTab.tsx Toss UI/컴포넌트 제거 | (예정) |
| 2025-11-14 | S03 | Firebase Functions Toss 확인 (없음) | - |
| 2025-11-14 | S04 | ENV 파일 TOSS 변수 제거 | (예정) |
| 2025-11-14 | S05 | 문서 Toss 전제 수정 | (예정) |
| 2025-11-14 | S06 | 빌드 테스트 성공, 검증 완료 | - |
| 2025-11-14 | S07 | T2-6 작업완료보고서 생성 (본 파일) | (예정) |

---

**보고서 생성 완료 (2025-11-14) — T2-6: TossPayments 결제 연동 완전 제거 작업 종료**
