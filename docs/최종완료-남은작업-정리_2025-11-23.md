# 최종 완료까지 남은 작업 정리

**작성일**: 2025-11-23  
**목적**: 프로젝트 최종 완료를 위해 남은 작업들을 우선순위별로 정리

---

## 🔴 우선순위 1: 필수 기능 구현 (즉시 필요)

### 1.1 Firebase Storage 이미지 업로드 구현

**현재 상태**: 
- 메뉴 등록/수정 시 이미지 파일을 선택할 수 있지만, 실제 Firebase Storage에 업로드되지 않음
- 현재는 `URL.createObjectURL(file)`로 임시 blob URL 사용

**영향 범위**:
- `MenuCreateDialog.tsx` - 메뉴 등록 시 이미지 업로드
- `MenuEditDialog.tsx` - 메뉴 수정 시 이미지 업로드
- `ReviewWrite.tsx` - 리뷰 작성 시 사진 업로드

**필요 작업**:
- [ ] Firebase Storage 업로드 함수 구현 (`lib/storage.ts` 또는 유사)
- [ ] 이미지 최적화 (리사이징, 압축)
- [ ] 업로드 진행률 표시
- [ ] 에러 핸들링 (용량 초과, 형식 오류 등)
- [ ] 기존 이미지 삭제 로직 (수정 시)

**예상 작업 시간**: 2-3시간

---

### 1.2 메뉴 통계 (getMenuStats) Firebase 구현

**현재 상태**: 
- `getMenuStats()` 함수가 Mock 데이터만 반환
- 관리자 대시보드에서 실제 통계가 표시되지 않음

**필요 작업**:
- [ ] Firestore에서 메뉴 목록 조회
- [ ] 통계 계산 로직 구현:
  - total: 전체 메뉴 수
  - available: 판매 가능 메뉴 수
  - soldout: 품절 메뉴 수
  - timeLimited: 시간제 판매 메뉴 수
  - byCategory: 카테고리별 메뉴 수
- [ ] 에러 발생 시 Mock 모드로 fallback

**예상 작업 시간**: 1-2시간

---

### 1.3 옵션 그룹 ID → 객체 변환

**현재 상태**: 
- `MenuDoc`에 `optionGroups: string[]` (ID 배열)로 저장
- `Menu` 엔티티로 변환 시 실제 OptionGroup 객체로 변환 필요

**필요 작업**:
- [ ] `buildMenuFromDoc` 함수에서 optionGroups ID 배열을 실제 객체로 변환
- [ ] Firestore에서 optionGroups 컬렉션 조회
- [ ] 조인 로직 구현

**예상 작업 시간**: 1-2시간

---

## 🟡 우선순위 2: 중요 기능 구현 (단기)

### 2.1 통합 분석 (Integrated Analytics) Firebase 구현

**현재 상태**: 
- 모든 통합 분석 함수가 Mock 데이터 반환
- `getIntegratedKPI`, `getHourlyAnalysis`, `getDayOfWeekAnalysis` 등

**필요 작업**:
- [ ] Firestore aggregation 쿼리 구현
- [ ] 주문 데이터 집계
- [ ] 시간대별, 요일별 분석
- [ ] 메뉴 성과 분석
- [ ] 고객 행동 분석
- [ ] 쿠폰/포인트 효과 분석
- [ ] 리뷰 분석
- [ ] 배달 성과 분석
- [ ] 알림 효과 분석

**예상 작업 시간**: 4-6시간

---

### 2.2 관리자 분석 (Analytics) Firebase 구현

**현재 상태**: 
- `getKPIData`, `getHourlyOrders`, `getTopMenuSales`, `getDailySales`가 Mock 데이터 반환

**필요 작업**:
- [ ] Firestore aggregation 쿼리 구현
- [ ] KPI 데이터 집계
- [ ] 시간대별 주문 분석
- [ ] 인기 메뉴 판매 분석
- [ ] 일별 매출 분석

**예상 작업 시간**: 2-3시간

---

### 2.3 공지사항 (Notices) Firebase 구현

**현재 상태**: 
- 모든 공지사항 API 함수가 Mock 데이터 반환
- `getNotices`, `createNotice`, `updateNotice`, `deleteNotice`, `getNoticeById`

**필요 작업**:
- [ ] Firestore `notices` 컬렉션 구조 설계
- [ ] CRUD 함수 구현
- [ ] 공지사항 목록 조회 (페이지네이션)
- [ ] 공지사항 상세 조회
- [ ] 공지사항 생성/수정/삭제

**예상 작업 시간**: 2-3시간

---

### 2.4 설정 (Settings) Firebase 구현

**현재 상태**: 
- `getSettings`, `updateSettings`가 Mock 데이터 반환

**필요 작업**:
- [ ] Firestore `settings` 컬렉션 구조 설계
- [ ] 설정 조회/업데이트 함수 구현
- [ ] 설정 검증 로직

**예상 작업 시간**: 1-2시간

---

## 🟢 우선순위 3: 개선 사항 (중기)

### 3.1 리뷰 사진 Firebase Storage 업로드

**현재 상태**: 
- `ReviewWrite.tsx`에서 사진 업로드가 TODO로 남아있음

**필요 작업**:
- [ ] Firebase Storage 업로드 함수 호출
- [ ] 여러 사진 업로드 지원
- [ ] 업로드 진행률 표시

**예상 작업 시간**: 1-2시간

---

### 3.2 주문 추적 지도 API 연동

**현재 상태**: 
- `OrderTracking.tsx`에서 지도가 TODO로 남아있음

**필요 작업**:
- [ ] Kakao Maps 또는 Google Maps API 연동
- [ ] 배달원 위치 표시
- [ ] 실시간 위치 업데이트

**예상 작업 시간**: 3-4시간

---

### 3.3 배달 대행사 API 연동

**현재 상태**: 
- `lib/delivery/providers/providerA.ts`가 TODO로 남아있음

**필요 작업**:
- [ ] 실제 배달 대행사 API 엔드포인트 설정
- [ ] API 인증 구현
- [ ] 배달 요청/취소/상태 조회 구현
- [ ] 배달 추적 구현

**예상 작업 시간**: 4-6시간

---

### 3.4 PG 결제 연동

**현재 상태**: 
- `Checkout.tsx`에서 실제 PG 결제 연동이 TODO로 남아있음
- Cloud Functions에 NICEPAY 관련 코드는 있으나 미완성

**필요 작업**:
- [ ] NICEPAY 또는 다른 PG사 API 연동
- [ ] 결제 요청/승인/취소 구현
- [ ] 결제 실패 처리
- [ ] 결제 내역 조회

**예상 작업 시간**: 6-8시간

---

### 3.5 Cloud Functions 스케줄러 구현

**현재 상태**: 
- 포인트 만료, 쿠폰 만료 스케줄러가 TODO로 남아있음

**필요 작업**:
- [ ] `onScheduleExpirePoints` 함수 구현
- [ ] `onScheduleExpireCoupons` 함수 구현
- [ ] Cloud Scheduler 설정
- [ ] 매일 자동 실행 설정

**예상 작업 시간**: 2-3시간

---

### 3.6 FCM 푸시 알림 구현

**현재 상태**: 
- `lib/fcm.ts`와 Cloud Functions에 FCM 관련 코드가 있으나 미완성

**필요 작업**:
- [ ] 사용자별 FCM 토큰 저장 구조 설계
- [ ] 토큰 등록/삭제 구현
- [ ] 푸시 알림 전송 구현
- [ ] 주문 상태 변경 시 알림
- [ ] 리뷰 답글 알림
- [ ] 쿠폰 발급 알림

**예상 작업 시간**: 4-6시간

---

### 3.7 리뷰 신고 기능 Firebase 구현

**현재 상태**: 
- `reportReview` 함수가 Mock 데이터만 사용

**필요 작업**:
- [ ] Firestore `reviews/{reviewId}/reports` 서브컬렉션 구조 설계
- [ ] 신고 생성 함수 구현
- [ ] 중복 신고 방지
- [ ] 관리자 신고 목록 조회

**예상 작업 시간**: 2-3시간

---

### 3.8 사용자 정보 조인 개선

**현재 상태**: 
- 포인트 관리에서 사용자 정보(phone, name)가 undefined로 표시됨

**필요 작업**:
- [ ] `users` 컬렉션과 조인하여 phone/name 가져오기
- [ ] 포인트 내역에 사용자 정보 포함

**예상 작업 시간**: 1-2시간

---

## 🔵 우선순위 4: 최적화 및 개선 (장기)

### 4.1 성능 최적화

**필요 작업**:
- [ ] 이미지 lazy loading
- [ ] 코드 스플리팅 (dynamic import)
- [ ] Firestore 쿼리 최적화
- [ ] 캐싱 전략 구현

**예상 작업 시간**: 4-6시간

---

### 4.2 에러 핸들링 개선

**필요 작업**:
- [ ] 전역 에러 핸들러
- [ ] 에러 바운더리 추가
- [ ] 사용자 친화적 에러 메시지
- [ ] 에러 로깅 (Sentry 등)

**예상 작업 시간**: 2-3시간

---

### 4.3 테스트 코드 작성

**필요 작업**:
- [ ] 단위 테스트 (Jest/Vitest)
- [ ] 통합 테스트
- [ ] E2E 테스트 개선

**예상 작업 시간**: 8-12시간

---

### 4.4 문서화

**필요 작업**:
- [ ] API 문서화
- [ ] 컴포넌트 문서화
- [ ] 배포 가이드
- [ ] 운영 가이드

**예상 작업 시간**: 4-6시간

---

## 📊 작업 우선순위 요약

| 우선순위 | 작업 | 예상 시간 | 중요도 |
|---------|------|----------|--------|
| 🔴 1 | Firebase Storage 이미지 업로드 | 2-3h | 높음 |
| 🔴 1 | 메뉴 통계 Firebase 구현 | 1-2h | 높음 |
| 🔴 1 | 옵션 그룹 ID → 객체 변환 | 1-2h | 높음 |
| 🟡 2 | 통합 분석 Firebase 구현 | 4-6h | 중간 |
| 🟡 2 | 관리자 분석 Firebase 구현 | 2-3h | 중간 |
| 🟡 2 | 공지사항 Firebase 구현 | 2-3h | 중간 |
| 🟡 2 | 설정 Firebase 구현 | 1-2h | 중간 |
| 🟢 3 | 리뷰 사진 업로드 | 1-2h | 낮음 |
| 🟢 3 | 지도 API 연동 | 3-4h | 낮음 |
| 🟢 3 | 배달 대행사 API | 4-6h | 낮음 |
| 🟢 3 | PG 결제 연동 | 6-8h | 낮음 |
| 🟢 3 | Cloud Functions 스케줄러 | 2-3h | 낮음 |
| 🟢 3 | FCM 푸시 알림 | 4-6h | 낮음 |
| 🟢 3 | 리뷰 신고 Firebase | 2-3h | 낮음 |
| 🟢 3 | 사용자 정보 조인 | 1-2h | 낮음 |
| 🔵 4 | 성능 최적화 | 4-6h | 낮음 |
| 🔵 4 | 에러 핸들링 개선 | 2-3h | 낮음 |
| 🔵 4 | 테스트 코드 | 8-12h | 낮음 |
| 🔵 4 | 문서화 | 4-6h | 낮음 |

**총 예상 시간**: 약 50-80시간

---

## 🎯 즉시 시작 가능한 작업 (우선순위 1)

### 작업 1: Firebase Storage 이미지 업로드

**파일**: `lib/storage.ts` (신규 생성)

```typescript
import { getStorage, ref, uploadBytes, getDownloadURL } from 'firebase/storage';

export async function uploadImage(file: File, path: string): Promise<string> {
  const storage = getStorage();
  const storageRef = ref(storage, path);
  await uploadBytes(storageRef, file);
  return await getDownloadURL(storageRef);
}
```

**수정 필요 파일**:
- `MenuCreateDialog.tsx` - `uploadImage` 함수 교체
- `MenuEditDialog.tsx` - `uploadImage` 함수 교체
- `ReviewWrite.tsx` - 사진 업로드 로직 추가

---

### 작업 2: 메뉴 통계 Firebase 구현

**파일**: `lib/admin/menus.api.ts`

**수정 위치**: `getMenuStats` 함수 (약 625줄)

```typescript
export async function getMenuStats(storeId: string): Promise<MenuStats> {
  if (!USE_FIREBASE) {
    return getMenuStatsMock(storeId);
  }

  try {
    const menus = await getMenus({}, storeId);
    // 통계 계산 로직
    // ...
  } catch (error) {
    console.warn('[getMenuStats] Firebase error, falling back to mock');
    return getMenuStatsMock(storeId);
  }
}
```

---

### 작업 3: 옵션 그룹 변환

**파일**: `lib/admin/menus.api.ts`

**수정 위치**: `buildMenuFromDoc` 함수 (약 137줄)

```typescript
// optionGroups: [], // TODO: optionGroups ID 배열을 실제 OptionGroup 객체로 변환
const optionGroups = await Promise.all(
  (doc.optionGroups || []).map(id => getOptionGroupById(id))
);
```

---

## 📝 체크리스트

### 즉시 시작 가능한 작업
- [ ] Firebase Storage 이미지 업로드 구현
- [ ] 메뉴 통계 Firebase 구현
- [ ] 옵션 그룹 ID → 객체 변환

### 단기 작업 (1주일 내)
- [ ] 통합 분석 Firebase 구현
- [ ] 관리자 분석 Firebase 구현
- [ ] 공지사항 Firebase 구현
- [ ] 설정 Firebase 구현

### 중기 작업 (1개월 내)
- [ ] 리뷰 사진 업로드
- [ ] 지도 API 연동
- [ ] 배달 대행사 API
- [ ] PG 결제 연동
- [ ] Cloud Functions 스케줄러
- [ ] FCM 푸시 알림

### 장기 작업 (향후)
- [ ] 성능 최적화
- [ ] 에러 핸들링 개선
- [ ] 테스트 코드
- [ ] 문서화

---

**작성자**: AI Assistant  
**최종 업데이트**: 2025-11-23

